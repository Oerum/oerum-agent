# Local agents instructions

Shared state in `.brain/`. MCP server: **oerum-agent** (`brain mcp`).

- Session start: `brain_resume`
- Milestones: `brain_checkpoint`
- Tracking: `brain_decision`, `brain_task`
- CLI fallback: `brain resume`, `brain checkpoint "note"`
