# Claude Code Integration

- Resume context in repository root with `brain resume`.
- Create snapshots during handoff moments with `brain checkpoint "handoff note"`.
- Keep `.brain/` tracked except transient lock/session paths.
